import { FormateadorMilesDirective } from './formateador-miles.directive';

describe('FormateadorMilesDirective', () => {
  it('should create an instance', () => {
    const directive = new FormateadorMilesDirective();
    expect(directive).toBeTruthy();
  });
});
